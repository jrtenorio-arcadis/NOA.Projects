name: Export and Manage Power Platform Solution

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted  # Windows self-hosted runner

    steps:
      # 1. Ensure nuget.config is available to NuGet in TEMP (fixes primarySources error)
      - name: Copy nuget.config to TEMP
        run: |
          copy "$env:APPDATA\NuGet\nuget.config" "C:\WINDOWS\TEMP\nuget.config"

      # 2. OPTIONAL: Add nuget.org source explicitly in TEMP in case copy fails
      - name: Add nuget.org source in TEMP
        run: |
          C:\Tools\NuGet\nuget.exe sources Add -Name nuget.org -Source https://api.nuget.org/v3/index.json -ConfigFile C:\WINDOWS\TEMP\nuget.config

      # 3. OPTIONAL: Install Power Platform CLI via npm as fallback
      # - name: Install Power Platform CLI via npm
      #   run: npm install -g pac

      # 4. Install Power Platform Tools (uses NuGet and CLI)
      - name: Install Power Platform Tools
        uses: microsoft/powerplatform-actions/actions-install@v1

      # 5. Export Managed Solution
      - name: Export Solution
        uses: microsoft/powerplatform-actions/export-solution@v1
        with:
          environment-url: 'https://org82c8d52d.crm.dynamics.com'
          user-name: 'GECSPAdmin@arcadis.com'
          password-secret: ${{ secrets.NOA_PP_SECRET }}
          solution-name: NOA.ReusableComponents.Solution
          solution-output-file: 'NOA.ReusableComponents.Solution.zip'
          working-directory: 'C:/Users/Administrator/out'

      # 6. Unpack Managed Solution for archival (not editable source)
      - name: Unpack Solution
        uses: microsoft/powerplatform-actions/unpack-solution@v1
        with:
          solution-file: 'C:/Users/Administrator/out/NOA.ReusableComponents.Solution.zip'
          solution-folder: 'C:/Users/Administrator/out/solutions/NOAReusableComponentsSolution'
          solution-type: 'Managed'
          overwrite-files: true

      # 7. Publish Solution (optional unless importing/updating)
      - name: Publish Solution
        uses: microsoft/powerplatform-actions/publish-solution@v1
        with:
          environment-url: 'https://org82c8d52d.crm.dynamics.com'
          user-name: 'GECSPAdmin@arcadis.com'
          password-secret: ${{ secrets.NOA_PP_SECRET }}

      # 8. Prepare unpacked solution artifacts for check-in (managed = archive only)
      - name: Prepare solution changes for check-in into source control
        uses: microsoft/powerplatform-actions/branch-solution@v1
        with:
          solution-folder: 'C:/Users/Administrator/out/solutions/NOAReusableComponentsSolution'
          solution-target-folder: 'src/solutions/NOAReusableComponentsSolution'
          token: ${{ secrets.GITHUB_TOKEN }}
