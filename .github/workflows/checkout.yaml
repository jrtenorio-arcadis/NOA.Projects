on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted  
 
    steps:
      # 1️⃣ Checkout first
      - name: Checkout repository
        uses: actions/checkout@v3
