on:
  workflow_dispatch:

jobs:
  UPLOAD-TO-REPOSITORY:
    runs-on: self-hosted  
 
    steps:
      # 1️⃣ Checkout first
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: List files
        run: ls -la


